# Frontend Environment Configuration
# Copy this file to .env for local development

# =============================================================================
# API CONFIGURATION
# =============================================================================
# Backend API base URL
# - For local development (running backend directly): http://localhost:8000/api
# - For Docker deployment: leave unset (defaults to /api, proxied through nginx)
VITE_API_BASE=http://localhost:8000/api
